export type BlogParams = {
    id: string
};

export type CreateBlog= {
    name: string,
    description: string,
    websiteUrl: string
};

export type UpdateBlog= {
    name: string,
    description: string,
    websiteUrl: string
};